# Карта Modbus-сигналов RO-установки

> **Источники:**
> - `doc/re_urg2kmm2.pdf` — РЭ УРЖ2КМ Модель 2 (ТЕСС 421457.015 РЭ)
> - `doc/sl21.pdf` — РЭ СЛ21
> - `doc/wsh ai.txt` — Development Protocol V2 для **Modbus RTU Analog Input 8CH** (заводской документ)
> - `doc/waveshare_ai_ref/Modbus_RTU_Analog_Input.ino` + `modbus_crc.h` — Arduino-демо производителя (подтверждает FC 0x04, 8 регистров, big-endian uint16)
> - KWS-306L — карта регистров от заказчика (см. §5)
>
> **Связанные документы:**
> - [`modbus_addresses.md`](modbus_addresses.md) — назначенные slave-адреса и инструкции их смены
> - `components/modbus_poller/modbus_poller.c` — реализация опросчика
> - `components/board_hal/include/board_config.h` — макросы `MB_ADDR_*`

---

## 1. Топология шины RS-485

```
  ESP32-S3 (master) ── UART1/RS-485 (RE/DE)
        │
        ├── slave 1   Waveshare Analog Input 8CH    (8× 4–20 мА для датчиков давления/уровня/t°)
        ├── slave 2   УРЖ2КМ 4-канальный            (расход + объём × 4 канала)
        ├── slave 10  СЛ21-100Т #1                  (X1+X2 проводимость + t1+t2)
        ├── slave 11  СЛ21-100Т #2                  (X1+X2 проводимость + t1+t2)
        ├── slave 20  KWS-306L 3-фазный (НД-насос)  (V/I/P/E/T + реле)
        └── slave 21  KWS-306L 3-фазный (ВД-насос)  (V/I/P/E/T + реле)
```

| Параметр шины | Значение |
|---|---|
| Скорость | 9600 бод |
| Формат | 8N1 |
| Function codes используемые | 0x03 (read holding), 0x04 (read input), 0x06 (write single), 0x10 (write multiple) |

> Полная инструкция по смене адресов и параметров связи каждого устройства — в [`modbus_addresses.md`](modbus_addresses.md).

---

## 2. СЛ21-100Т (анализатор удельной электропроводности) — 2 шт.

**Источник:** `doc/sl21.pdf`, раздел 5.16 «МВ интерфейс».

### 2.1 Параметры

| Параметр | Значение в РЭ |
|---|---|
| Function code | **0x03 (Read Holding Registers)** только чтение |
| Default slave | 5 |
| Назначено | **10 и 11** (см. `modbus_addresses.md`) |
| Каналов на блок | **2 ячейки проводимости (X1, X2) + 2 термодатчика (t1, t2)** |

### 2.2 Карта регистров одного блока

| Holding (zero-based) | Holding (Modicon) | Имя | Тип | Восстановление физ. величины |
|---:|---:|---|---|---|
| 0x0000 | 40001 | X1 LO | uint16 | `cond_uS = ((HI<<16) \| LO) / 100`<br/>(СЛ21-**100Т**, формат `XXXXXX.XX × 100`) |
| 0x0001 | 40002 | X1 HI | uint16 | (HI вместе с LO выше) |
| 0x0002 | 40003 | t1    | int16  | `t°C = raw / 10` |
| 0x0003 | 40004 | X2 LO | uint16 | `cond_uS = ((HI<<16) \| LO) / 100` |
| 0x0004 | 40005 | X2 HI | uint16 | (HI вместе с LO выше) |
| 0x0005 | 40006 | t2    | int16  | `t°C = raw / 10` |

> ⚠️ **Word-order:** младшее слово в МЛАДШЕМ адресе (40001 = LO, 40002 = HI). Это **противоположно** дефолтному поведению ESP-IDF mbc_master. Поэтому в `conductivity.c` мы читаем 6 регистров как `uint16[6]` и собираем 32-битное значение вручную:
> ```c
> uint32_t raw = ((uint32_t)reg[1] << 16) | reg[0];
> float cond_uS = (float)raw / 100.0f;
> ```

### 2.3 Делитель в зависимости от модификации СЛ21

| Модель | Формула индикатора | Дивизор кода |
|---|---|---:|
| СЛ21-1Т | `XXXX.XXXX мкСм/см × 10000` | 10000 |
| СЛ21-10Т | `XXXXX.XXX мкСм/см × 1000` | 1000 |
| **СЛ21-100Т** ← **наш случай** | `XXXXXX.XX мкСм/см × 100` | **100** |

В коде: `SL21_COND_DIVISOR = 100` в `components/drivers/conductivity.c`. ✅

### 2.4 Логические каналы — 4 точки контроля проводимости

Один блок СЛ21 = 2 ячейки. Два блока = **4 канала проводимости + 4 канала температуры**.

| Логический канал | Slave | Регистр | Точка измерения |
|---:|---:|---|---|
| `COND_CH_FEED` | 11 | X1 + t1 | питательная вода (вход насоса НД) |
| `COND_CH_PERM1` | 11 | X2 + t2 | пермеат 1-й ступени |
| `COND_CH_PERM2` | 10 | X1 + t1 | пермеат 2-й ступени (товарный) |
| `COND_CH_CONC` | 10 | X2 + t2 | концентрат (после мембран) |

> **Текущая реализация:** в `modbus_poller.c` slave 11 опрашивает только 3 регистра (X1+t1) — нужно расширить до 6, чтобы получить 4-й канал. См. §7.

---

## 3. УРЖ2КМ (4-канальный ультразвуковой расходомер) — 1 шт.

**Источник:** `doc/re_urg2kmm2.pdf` + наблюдаемая семантика из `components/drivers/flowmeter.c`.

> ⚠️ **Полной нормативной карты регистров в РЭ нет** — производитель ссылается на отдельный документ «ModBus Universal» на сайте ТЕСС (п. 2.2.8.5.1 РЭ). Адреса ниже — наблюдаемые в существующей реализации, **должны быть верифицированы** на стенде через ModBus Universal.

### 3.1 Параметры

| Параметр | Значение |
|---|---|
| Тип прибора | УРЖ2КМ, **4-канальный модификация** (подтверждено заказчиком) |
| Function code | 0x03 (Read Holding) — основной |
| Default slave | 1 |
| Назначено | **2** |
| Скорость обмена | программируется: 1.2 / 2.4 / 4.8 / 9.6 / 14.4 / 19.2 КБод; **используем 9600** |
| Тип протокола | RTU / ASCII; **используем RTU** |

### 3.2 Формат данных — word-swapped IEEE 754 float

Каждый канал расхода и каждый канал объёма передаётся как **2 регистра**:
```
addr+0  → младшее слово (mantissa low)        ← LO
addr+1  → старшее слово (sign+exponent+mantissa hi)  ← HI
```
Внутри регистра — стандартный Modbus big-endian (байт hi → byte lo). Сборка float:
```c
uint32_t ieee = ((uint32_t)reg_hi << 16) | (uint32_t)reg_lo;
float result;
memcpy(&result, &ieee, sizeof(float));
```
(см. `flowmeter.c::urzh_regs_to_float`).

### 3.3 Карта регистров (4 канала)

| Holding | Имя | Тип | Канал | Ед. изм. |
|---:|---|---|---:|---|
| 0x0000–0x0001 | F1 (расход 1 канал) | float-WS | 1 | м³/ч |
| 0x0002–0x0003 | F2 (расход 2 канал) | float-WS | 2 | м³/ч |
| 0x0004–0x0005 | F3 (расход 3 канал) | float-WS | 3 | м³/ч |
| 0x0006–0x0007 | F4 (расход 4 канал) | float-WS | 4 | м³/ч |
| 0x0036–0x0037 | V1 (объём 1 канал) | float-WS | 1 | м³ |
| 0x0038–0x0039 | V2 | float-WS | 2 | м³ |
| 0x003A–0x003B | V3 | float-WS | 3 | м³ |
| 0x003C–0x003D | V4 | float-WS | 4 | м³ |
| 0x003E–0x003F | V5 ⚠️ | float-WS | (?) | (?) |
| 0x0040–0x0041 | V6 ⚠️ | float-WS | (?) | (?) |
| 0x0042–0x0043 | V7 ⚠️ | float-WS | (?) | (?) |
| 0x0044–0x0045 | V8 ⚠️ | float-WS | (?) | (?) |

⚠️ **Регистры 0x003E..0x0045** — текущий код читает 16 регистров объёма (8 float), но физических каналов всего 4. Возможные интерпретации:
- V1..V4 = текущие накопленные объёмы по 4 каналам
- V5..V8 = объёмы за предыдущий период (час/сутки), либо T1..T4 (время наработки), либо «состояние НОРМА/ОТКАЗ», либо «реверсивные накопители» если стоит исполнение `R`
- **Нужна сверка с ModBus Universal.** До сверки — игнорировать V5..V8.

### 3.4 Логические каналы — привязка к точкам учёта

| Канал УРЖ | Регистр расхода | Регистр объёма | Точка установки |
|---:|---|---|---|
| 1 | 0x0000 | 0x0036 | питательная вода (вход) |
| 2 | 0x0002 | 0x0038 | пермеат товарный |
| 3 | 0x0004 | 0x003A | концентрат (дренаж) |
| 4 | 0x0006 | 0x003C | дренаж промывки / резерв |

> Конкретное физическое подключение (какой ПЭП на какую трубу) согласовать с монтажом. Распределение выше — **рекомендация для двухступенчатого RO**.

### 3.5 Регистры конфигурации (из меню, табл. программирования)

| Параметр | Меню | Значение | Modbus addr |
|---|---|---|---|
| Сетевой адрес | подрежим 05 | **2** | TBD |
| Скорость | подрежим 04 | **9600** | TBD |
| Тип протокола | подрежим 03 | **RTU** | TBD |
| Шкала канала, м³/ч | подрежимы 12..42 | по типоразмеру трубы | TBD |
| Диаметр трубы | подрежимы 13..43 | по монтажу | TBD |
| Уровень отсечки малых расходов | подрежимы 15..45 | 0.1..26 % от шкалы | TBD |
| Постоянная усреднения | подрежимы 16..46 | 0.02..80 с | TBD |

Адреса для записи через Modbus в РЭ не приведены — настраивается с лицевой панели или через ModBus Universal.

---

## 4. Waveshare Modbus RTU **Analog Input 8CH** — 1 шт.

**Источники:**
- Заводской протокол: `doc/wsh ai.txt` (Development Protocol V2)
- Wiki: https://www.waveshare.com/wiki/Modbus_RTU_Analog_Input_8CH
- Arduino-демо от производителя: `doc/waveshare_ai_ref/Modbus_RTU_Analog_Input.ino` (+ `modbus_crc.h`)

### 4.1 Параметры

| Параметр | Значение |
|---|---|
| Default slave | 1 |
| Назначено | **1** (без изменений) |
| Default baud | 9600 |
| Function codes | 0x03, 0x04, 0x06, 0x10 |
| Каналов | **8 аналоговых входов** (4–20 мА / 0–10 В / scale code, конфигурируется) |
| Разрешение АЦП | 12 bit |

Версии модуля:

| Версия | Default режим | Sampling R | Op-Amp ratio |
|---|---|---:|---:|
| Modbus RTU Analog Input 8CH (A) ← **наш** | 4–20 mA (jumper closed) | 249 Ω | 32.4/49.9 |
| Modbus RTU Analog Input 8CH (B) | 0–10 V (jumper open) | 499 Ω | 10/32.4 |

### 4.2 Карта регистров

**Чтение 8 каналов одним фреймом** (FC 0x04, подтверждено Arduino-демо):

```
Команда: 01 04 00 00 00 08 F1 CC
         └┬┘└┬┘└──┬──┘└──┬──┘└─┬─┘
       slave│  start  count  CRC
            FC=04
            (read input)
```

| Channel | Input Register (FC 0x04) | Тип | Что хранится |
|:---:|:---:|---|---|
| 1 | **3x0000** | uint16 BE | сырое значение канала 1 |
| 2 | **3x0001** | uint16 BE | сырое значение канала 2 |
| 3 | **3x0002** | uint16 BE | сырое значение канала 3 |
| 4 | **3x0003** | uint16 BE | сырое значение канала 4 |
| 5 | **3x0004** | uint16 BE | сырое значение канала 5 |
| 6 | **3x0005** | uint16 BE | сырое значение канала 6 |
| 7 | **3x0006** | uint16 BE | сырое значение канала 7 |
| 8 | **3x0007** | uint16 BE | сырое значение канала 8 |

> Big-endian — старший байт первым. Из Arduino-демо:
> ```c
> input[i] = (buf[3 + i*2] * 0x100) + buf[4 + i*2];
> ```

**Что значит «сырое значение»** — зависит от режима канала (см. §4.3):
- В **режиме 2 (0–20 mA)** или **3 (4–20 mA)** raw возвращается **в микроамперах**:
  ```c
  float current_mA = (float)raw / 1000.0f;     // 20.000 mA = 20000 µA
  ```
- В **режиме 0 (0–5 V)** или **1 (1–5 V)** raw — **в милливольтах**:
  ```c
  float voltage_V = (float)raw / 1000.0f;       // 5.000 V = 5000 mV
  ```
- В **режиме 4 (scale code)** raw — это сырое 12-битное значение АЦП (0..4095):
  ```c
  float V = scale * 3300.0f / 4095.0f / op_amp_ratio;
  float I_A = V / R_sense;     // R_sense = 249 Ω для (A), 499 Ω для (B)
  ```

### 4.3 Holding-регистры конфигурации

| Адрес (HEX) | Имя | Тип | R/W | FC |
|---:|---|---|---|---|
| 4x1000 | Channel 1 mode | uint16 (0..4) | RW | 0x03/0x06/0x10 |
| 4x1001 | Channel 2 mode | uint16 | RW | то же |
| ... | ... | ... | ... | ... |
| 4x1007 | Channel 8 mode | uint16 | RW | то же |
| 4x2000 | UART params | uint16 | RW (0x03/0x06) | HI=parity (0=None/1=Odd/2=Even); LO=baud code |
| 4x4000 | Device address | uint16 | RW (0x03/0x06) | 0x0001..0x00FF |
| 4x8000 | Software version | uint16 | R (0x03) | BCD/100 (0x0064=V1.00) |

**Режимы каналов (4x1000+ch, версия A):**

| Mode | Диапазон | Назначение |
|---:|---|---|
| 0 | 0–5 V | напряжение, для датчиков 0–5 В |
| 1 | 1–5 V | напряжение, «живой ноль» |
| 2 | 0–20 mA | ток, без живого нуля |
| **3** | **4–20 mA** ⬅︎ **наш default для датчиков давления/уровня** | стандартная промышленная токовая петля |
| 4 | 4096-scale code | сырое АЦП 0..4095 |

**Коды baudrate (LO of 4x2000):**

| Code | Baud |
|---:|---:|
| 0x00 | 4800 |
| 0x01 | 9600 ← наш |
| 0x02 | 19200 |
| 0x03 | 38400 |
| 0x04 | 57600 |
| 0x05 | 115200 |
| 0x06 | 128000 |
| 0x07 | 256000 |

### 4.4 One-time setup (выполнить один раз при первом запуске или после reset настроек)

```
# Перевести ВСЕ 8 каналов в режим 3 (4–20 mA) одним FC 0x10
01 10 10 00 00 08 10 00 03 00 03 00 03 00 03 00 03 00 03 00 03 00 03 [CRC]

# Альтернатива по одному каналу (FC 0x06):
01 06 10 00 00 03 [CRC]   # Channel 1 → 4–20 mA
01 06 10 01 00 03 [CRC]   # Channel 2 → 4–20 mA
...
01 06 10 07 00 03 [CRC]   # Channel 8 → 4–20 mA

# Проверить чтением:
01 03 10 00 00 08 [CRC]
# Ответ должен быть 8× 0x0003

# Прочитать версию ПО (sanity check)
01 03 80 00 00 01 [CRC]   →   01 03 02 00 64 [CRC]   (V1.00)
```

> ⚠️ **Важно:** перед сменой режима с current на voltage (или наоборот) нужно **физически перенастроить джампер** на плате модуля (см. wiki, раздел Hardware Description). Иначе показания будут некорректны.

### 4.5 Конвертация raw → физическая величина (для нашего случая mode 3)

Для датчика 4–20 мА с диапазоном `[range_min .. range_max]` (например, 0–10 бар, 0–25 бар):

```c
// raw из input register, режим = 3 (4–20 mA), значение в µA
float raw_mA = (float)raw_uint16 / 1000.0f;

// Линейная конверсия 4-20 мА → physical
float physical = (raw_mA - 4.0f) / 16.0f * (range_max - range_min) + range_min;

// Граничные проверки:
if (raw_mA < 3.5f) {
    // обрыв линии (sensor fault) — токовая петля разорвана
    return NAN;
}
if (raw_mA > 20.5f) {
    // короткое замыкание / переходный процесс
    return NAN;
}
```

### 4.6 Логические каналы — привязка к точкам РО-установки

| Канал AI | Сигнал | Диапазон | Регистр | Ед. изм. |
|---:|---|---|---:|---|
| 1 | Давление **до НД-насоса** (P1) | 0–10 бар | `IN 0x0000` | бар |
| 2 | Давление **после НД-насоса / до фильтра** (P2) | 0–10 бар | `IN 0x0001` | бар |
| 3 | Давление **до ВД-насоса** (P3) | 0–10 бар | `IN 0x0002` | бар |
| 4 | Давление **после ВД-насоса / на мембраны** (P4) | 0–25 бар | `IN 0x0003` | бар |
| 5 | Температура питательной воды (4–20мА от Pt100-преобразователя) | 0–60 °C | `IN 0x0004` | °C |
| 6 | Уровень в баке промывки (датчик 4–20 мА) | 0–100 % | `IN 0x0005` | % |
| 7 | Резерв | — | `IN 0x0006` | — |
| 8 | Резерв | — | `IN 0x0007` | — |

> Конкретный маппинг (диапазоны и точки) — согласовать с `components/process/include/process_signals.h` (текущий `interlocks.h`) и реальным монтажом.

---

## 5. KWS-306L (трёхфазный счётчик электроэнергии) — 2 шт.

**Источник:** карта регистров от заказчика. ⚠️ **Карта не полная** — для 3-фазного счётчика типично есть фазные напряжения U1/U2/U3, фазные токи I1/I2/I3, реактивная/полная мощность Q/S, частота Hz, PF; их нужно дополнить через datasheet поставщика.

### 5.1 Параметры

| Параметр | Значение |
|---|---|
| Default slave | 1 |
| Назначено | **20** (НД-насос), **21** (ВД-насос) |
| Default baud | 9600 (типовое для KWS-серии) |
| Function code | 0x03 (read holding), 0x06 (write single — для управления реле) |

### 5.2 Карта регистров (от заказчика)

| Holding (HEX) | Holding (DEC) | Параметр | Тип | Множитель → физ. ед. |
|---:|---:|---|---|---|
| 0x000E | 14 | Voltage (фазное?) | uint16 | × 0.1 → В (например, 2300 = 230.0 В) |
| 0x0010 | 16 | Current | uint16 | × 0.001 → А (например, 500 = 0.5 А) |
| 0x0012 | 18 | Power (active) | uint16 | × 0.1 → Вт |
| 0x001A | 26 | Energy | uint16 ⚠️ | × 0.01 → кВт·ч (иногда × 0.001) |
| 0x001B | 27 | Temperature | uint16 | × 1 → °C |
| 0x003F | 63 | Реле управления | uint16 | 1 = Вкл, 0 = Выкл (FC 0x06) |

⚠️ **Открытые вопросы по карте:**
1. **Energy = uint16?** При множителе 0.01 кВт·ч это даёт максимум 655 кВт·ч и переполнение. Скорее всего, energy = uint32 (2 регистра 0x001A + 0x001B), а температура — где-то в другом месте. Нужен datasheet.
2. **Voltage** — это сумма фаз, фаза A, или среднее? Для 3-фазного счётчика нужны 3 регистра (U1/U2/U3) для контроля перекоса фаз.
3. **Current** — то же: для 3-фазного нужны I1/I2/I3.
4. **Power** — активная P, реактивная Q, полная S? Один регистр × 0.1 Вт даёт max ~6.5 кВт — мало для ВД-насоса 4..7.5 кВт. Возможно P = uint32 в 2 регистрах.

**Адреса 0x0000..0x000D и 0x0013..0x0019** — не описаны в полученной карте. Они либо зарезервированы, либо содержат фазные значения. **Действие:** при первом подключении опросить блок 0x0000..0x0040 целиком, сравнить с показаниями LCD прибора, восстановить полную карту.

### 5.3 Команды

```c
// Прочитать все известные параметры одним фреймом (0x000E..0x001B = 14 регистров)
01 03 00 0E 00 0E [CRC]

// Включить реле (например, размыкание — отключение нагрузки KWS = аварийное отключение насоса)
01 06 00 3F 00 00 [CRC]

// Включить реле обратно
01 06 00 3F 00 01 [CRC]
```

⚠️ **Реле KWS как защитный размыкатель** — отдельный механизм аварийного обесточивания насоса. Использовать только при критичных алармах (E-STOP, перегрев насоса по температуре в KWS reg 0x001B). НЕ использовать как штатный SAFE-shutoff — для этого есть DO TCA9554.

### 5.4 Логические сигналы

| Сигнал | Slave | Регистр | Применение в SM |
|---|---:|---|---|
| Voltage НД | 20 | 0x000E | Если < 200 V или > 250 V → ALARM 1PH_VOLTAGE_OOR (для 1-фазного НД-насоса) |
| Current НД | 20 | 0x0010 | Если == 0 в состоянии AUTO_RUNNING → INTERLOCK_PUMP1_NO_CURRENT (зам. 4-20мА давления) |
| Power НД | 20 | 0x0012 | Учёт kWh, аномальное P > 1.2 × P_nominal → перегруз |
| Energy НД | 20 | 0x001A | Накопление в NVS, публикация в MQTT для биллинга |
| Temperature НД | 20 | 0x001B | Если > 80 °C → ALARM_OVER_TEMP, размыкаем реле KWS |
| Voltage/Current/Power/Energy/Temp ВД | 21 | 0x000E..0x001B | то же для ВД-насоса |

---

## 6. Сводная карта сигналов установки

### 6.1 Аналоговые 4–20 мА (через Waveshare Analog Input 8CH, slave 1)

См. §4.6 — 8 каналов: P1..P4 (давление), t° питательной, уровень бака промывки, 2 резерва. Конкретный маппинг согласовать с `process_signals.h` и монтажом.

### 6.2 Расход и накопленный объём (УРЖ2КМ slave 2, 4 канала)

| УРЖ канал | Сигнал | F-регистр | V-регистр |
|---:|---|---|---|
| 1 | Поток питательной воды | 0x0000 | 0x0036 |
| 2 | Поток пермеата товарного | 0x0002 | 0x0038 |
| 3 | Поток концентрата | 0x0004 | 0x003A |
| 4 | Поток дренажа промывки | 0x0006 | 0x003C |

### 6.3 Проводимость и температура (СЛ21-100Т ×2, slaves 10 & 11)

| Логический канал | Slave | Ячейка | Холдинги (cond + temp) |
|---|---:|:---:|---|
| `COND_CH_FEED` (питательная) | 11 | X1 / t1 | 0x0000–0x0001 / 0x0002 |
| `COND_CH_PERM1` (пермеат 1ст.) | 11 | X2 / t2 | 0x0003–0x0004 / 0x0005 |
| `COND_CH_PERM2` (пермеат 2ст.) | 10 | X1 / t1 | 0x0000–0x0001 / 0x0002 |
| `COND_CH_CONC` (концентрат) | 10 | X2 / t2 | 0x0003–0x0004 / 0x0005 |

### 6.4 Электропитание насосов (KWS-306L ×2, slaves 20 & 21)

| Сигнал | Slave 20 (НД) | Slave 21 (ВД) |
|---|---|---|
| Напряжение | 0x000E | 0x000E |
| Ток | 0x0010 | 0x0010 |
| Мощность | 0x0012 | 0x0012 |
| Энергия | 0x001A | 0x001A |
| Температура | 0x001B | 0x001B |
| Реле | 0x003F (RW) | 0x003F (RW) |

### 6.5 Дискретные выходы (DO через TCA9554, **не Modbus**)

> Подтверждено заказчиком: **дозатор управляется через DO** (релейный выход на TCA9554), не через Modbus и не через AO.

Текущая карта DO (см. `components/board_hal/include/board_config.h` или `process_signals.h`):
- DO0 — клапан подачи питательной
- DO1 — клапан перед мембранами
- DO2 — насос НД (вкл/выкл) — дублирует контроль через KWS-306L #20
- DO3 — насос ВД (вкл/выкл) — дублирует через KWS-306L #21
- DO4 — клапан дренажа концентрата
- DO5 — клапан промывки
- DO6 — **дозатор реагента / биоцида** (импульсное вкл/выкл по таймеру)
- DO7 — резерв (зуммер? световой сигнал?)

> Логику дозатора в режиме WASHING — см. TODO в `README.md`.

---

## 7. Гэп-анализ текущей реализации

| # | Проблема | Файл | Приоритет |
|---|---|---|---|
| 1 | СЛ21 slave 11 опрашивает только 3 регистра (X1+t1) — теряем X2/t2, не получаем 4-й канал проводимости | `modbus_poller.c:171`, `conductivity.c` | **Высокий** |
| 2 | **Формула `raw / 4095` в `analog_input.c` — для режима 4 (scale code), даёт относительное значение 0..1, не подходит для 4–20 мА.** Адресация (8 регистров FC 0x04) ВЕРНА. | `analog_input.c:114` | **Высокий** — без правки давления интерпретируются как 0..100 % шкалы |
| 3 | **Нет one-time setup перевода каналов в режим 3 (4–20 мА).** Нужно при старте записать `4x1000..4x1007 = 0x0003` через FC 0x10 (или 0x06 по одному). | `analog_input.c::analog_input_init` или новый `setup_devices()` | **Высокий** — без этого raw трактуется в зависимости от текущей настройки модуля (mode неизвестен) |
| 4 | Адреса УРЖ2КМ 0x0004..0x0007 (F3/F4) и 0x003E..0x0045 (V5..V8) не сверены с протоколом | `modbus_poller.c`, `flowmeter.c` | **Средний** |
| 5 | KWS-306L не опрашивается (нет `MB_ADDR_KWS_*`, нет CID, нет драйвера) | — | **Высокий** |
| 6 | Default baud СЛ21 = 19200, у нас 9600. Нужно через меню СЛ21 переключить в 9600 | развёртывание | **Средний** |
| 7 | Нет one-time setup для смены slave-адресов УРЖ2КМ (1→2), СЛ21 (5→10/11), KWS (1→20/21) | развёртывание | **Высокий** — блокер запуска |
| 8 | Нет драйвера `power_meter.c` для KWS — нет учёта kWh, нет защиты по температуре насоса, нет резервного `INTERLOCK_NO_CURRENT` | `components/drivers/` | **Высокий** |
| 9 | Конверсия raw → physical в `analog_input.c` не учитывает обрыв линии (raw < 3500 µA) и КЗ (raw > 20500 µA) | `analog_input.c` | **Средний** — sensor fault detection |

---

## 8. Закрытые вопросы (по уточнению заказчика)

| # | Вопрос | Ответ |
|---|---|---|
| 1 | «4 расходомера» — это 1 прибор × 4 канала или 2 прибора × 2 канала? | **1 прибор УРЖ2КМ × 4 канала** |
| 2 | KWS-306L 1-фазный или 3-фазный? | **3-фазный**, 2 шт. |
| 3 | Какая ячейка СЛ21 к какой точке привязана? | См. §2.4 — **slave 11 = питание+пермеат1, slave 10 = пермеат2+концентрат** |
| 4 | Какой именно модуль аналогового ввода у Waveshare? | **Modbus RTU Analog Input 8CH** (4–20 мА), модель (A). Подтверждено Arduino-демо производителя — `doc/waveshare_ai_ref/Modbus_RTU_Analog_Input.ino`. |
| 5 | Где взять datasheet KWS? | Карта получена частично от заказчика; полная — запросить у поставщика. См. §5.2 — открытые вопросы 1–4 |
| 7 | Дозатор через AO или DO? | **DO** (релейный выход TCA9554) |
| 9 | Можно ли менять адреса устройств? | **Да** — назначены в [`modbus_addresses.md`](modbus_addresses.md): УРЖ2КМ→2, СЛ21→10/11, KWS→20/21 |

## Открытые вопросы

| # | Вопрос | Кому |
|---|---|---|
| B | Полная карта KWS-306L (фазные U1/U2/U3, I1/I2/I3, реактивная Q, частота, PF) — у поставщика | заказчик |
| C | Карта точек давления P1..P4 в `process_signals.h` — соответствует ли реальному монтажу (см. §4.6) | наладчик |
| D | Скорость шины: 9600 окончательно? (от этого зависит, нужно ли перенастраивать СЛ21) | заказчик |
| E | Диапазоны датчиков 4–20 мА: P1=0–10 бар, P4=0–25 бар, t°=0–60 °C, level=0–100 % — подтвердить по паспортам | наладчик |

---

## 9. Рекомендуемый план интеграции

### Этап 1 — Перенастройка устройств на стенде (физический доступ)

1. **УРЖ2КМ:** меню → подрежим 03 = RTU, подрежим 04 = 9600, подрежим 05 = `002` (адрес).
2. **СЛ21 #1:** меню → МВ интерфейс → Адрес = `010`, Скорость = 9600 бод, Паритет = чётный (если нет None).
3. **СЛ21 #2:** меню → МВ интерфейс → Адрес = `011`, Скорость = 9600 бод, Паритет = чётный.
4. **Waveshare Analog Input 8CH:** оставить адрес 1, baud 9600 (default), физически проверить что джамперы 4–20 мА замкнуты, через FC 0x10 записать `4x1000..4x1007 = 0x0003` (mode = 4–20 mA для всех 8 каналов) — см. §4.4.
5. **KWS-306L #1 (НД):** broadcast `00 06 40 00 00 14` (адрес 20).
6. **KWS-306L #2 (ВД):** при отключённом #1 — broadcast `00 06 40 00 00 15` (адрес 21).

### Этап 2 — Реверс-инжиниринг

1. **УРЖ2КМ:** прогнать ModBus Universal от ТЕСС, получить точную карту регистров F1..F4 / V1..V4 / T1..T4 / o1..o4. Зафиксировать в этом документе.
2. **KWS-306L:** опросить holding 0x0000..0x0040 через ModScan / `mbpoll`, сравнить с LCD прибора, восстановить полную карту U1/U2/U3, I1/I2/I3, P/Q/S, Hz, PF.

### Этап 3 — Изменения в коде

1. `components/board_hal/include/board_config.h`:
   ```c
   #define MB_ADDR_WAVESHARE_AI    1   /* Modbus RTU Analog Input 8CH (4–20 мА) */
   #define MB_ADDR_URZH2KM         2
   #define MB_ADDR_SL21_201        10
   #define MB_ADDR_SL21_101        11
   #define MB_ADDR_KWS_PUMP_LP     20  /* НОВОЕ */
   #define MB_ADDR_KWS_PUMP_HP     21  /* НОВОЕ */
   ```
2. `components/modbus_poller/include/modbus_poller.h`: новые CID:
   ```c
   typedef enum {
       CID_AI_CHANNELS = 0,          /* slave 1, input 0x0000..0x0007, 8 регистров (8 каналов 4–20мА) */
       CID_FLOW_RATES,               /* slave 2, holding 0x0000..0x0007, 8 регистров (4 float-WS) */
       CID_FLOW_VOLUMES,             /* slave 2, holding 0x0036..0x003D, 8 регистров (4 float-WS) — БЫЛО 16 */
       CID_COND_ADDR10,              /* slave 10, holding 0x0000..0x0005, 6 регистров */
       CID_COND_ADDR11,              /* slave 11, holding 0x0000..0x0005, 6 регистров — БЫЛО 3 */
       CID_KWS_PUMP_LP,              /* slave 20, holding 0x000E..0x001B, 14 регистров */
       CID_KWS_PUMP_HP,              /* slave 21, holding 0x000E..0x001B, 14 регистров */
       CID_COUNT
   } modbus_cid_t;
   ```
3. **Исправить `analog_input.c`:**
   - Заменить формулу `raw / 4095` на `raw / 1000` (µA → mA), затем линейная конверсия `(mA - 4) / 16 * (max - min) + min`.
   - Добавить sensor-fault detection: `raw < 3500` или `raw > 20500` → NaN + alarm `SENSOR_FAULT`.
   - Добавить one-time setup в `analog_input_init()` или в новой `mb_setup_devices()`: записать `4x1000..4x1007 = 0x0003` через FC 0x10 один раз при старте.
4. **Создать** `components/drivers/power_meter.c` для KWS-306L.
5. **Расширить** `conductivity.c` на 4 канала (вместо текущих 3).
6. **Сократить** `flowmeter.c` чтение объёмов с 16 до 8 регистров (V5..V8 пока пропустить до сверки с протоколом ТЕСС).

### Этап 4 — Интеграция в SM и алярмы

1. `interlocks.h`: новые биты `INTERLOCK_PUMP_LP_NO_CURRENT`, `INTERLOCK_PUMP_HP_NO_CURRENT`, `INTERLOCK_PUMP_OVERTEMP`, `INTERLOCK_VOLTAGE_OOR`.
2. `alarm_manager.h`: новые коды `ALARM_KWS_*`.
3. `state_machine.c::update_auto`: проверка current/voltage/temp от KWS перед переходом в RUNNING; авто-fault при перегреве.
4. Сверить диапазоны датчиков (P1..P4, t°, level) в `analog_input.c` с реальными паспортами 4–20 мА (открытый вопрос E).

### Этап 5 — Телеметрия

1. `mqtt_publish.c`: добавить топики `power/lp/{voltage,current,power,energy,temp}`, `power/hp/{...}`. Топики `ai/ch{1..8}` уже публикуются.
2. `web_api_status.c`: добавить новые поля JSON для энергопотребления.
3. HA discovery (`mqtt_discovery.c`): сенсоры для kWh, V, A, °C — для интеграции с Home Assistant.

---

## 10. Ссылки

- Назначенные slave-адреса и инструкции их смены: [`modbus_addresses.md`](modbus_addresses.md)
- Waveshare wiki Analog Input 8CH: https://www.waveshare.com/wiki/Modbus_RTU_Analog_Input_8CH
- Arduino-демо Analog Input 8CH: `doc/waveshare_ai_ref/`
- УРЖ2КМ ModBus Universal: на сайте «ТЕСС» (производитель)
- ESP-IDF Modbus master example: https://github.com/espressif/esp-modbus/tree/main/examples/serial_master
